'use client';

import AppShell from '@/components/AppShell';

export default function CustomersNewPage() {
  return (
    <AppShell pageTitle="Customers (New)">
      <div className="mb-6">
        <h1 className="text-2xl font-bold">Customers (New Version)</h1>
        <p className="mt-4">This is a test page to debug the routing issue.</p>
      </div>
    </AppShell>
  );
} 